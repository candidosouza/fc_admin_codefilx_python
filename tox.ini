[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    coverage
    pytest-cov
    Django
    djangorestframework
    model-bakery
commands =
    pytest --ignore __pypackages__
    coverage run -m pytest --ignore __pypackages__
    coverage xml